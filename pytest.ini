[pytest]
testpaths = tests
markers =
    smoke: critical tests that verify the API is alive
    negative: Tests that validate error handling and invalid input scenarios.
    boundary: Boundary value tests